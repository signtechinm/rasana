services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: rasana
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: rasana-local
    ports:
      - "5432:5432"
    volumes:
      - rasana-postgres:/var/lib/postgresql/data

volumes:
  rasana-postgres:
