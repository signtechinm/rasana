import Link from "next/link";
import { getCollection } from "@/lib/content";
import { SiteFooter, SiteHeader } from "@/components/SiteChrome";
export default async function BrandsPage() { const brands = await getCollection("brands"); return <><SiteHeader /><main className="brands-page"><section className="brands-hero"><div><p className="eyebrow gold">The brands we represent</p><h1>Our <em>brands.</em></h1><p>We take on brands we believe we can build in this market, and say no to the rest.</p></div></section><section className="brands-content"><div className="brands-intro"><p className="eyebrow">Selected partnerships</p><p>Each brand below is carried by Rasana in the UAE and, where the agreement allows, across the wider region. Enquire with our team to find the right line for your channel.</p></div><div className="product-grid">{brands.map((brand, index) => <Link className={`product-tile brand-tile brand-tile-${index % 3}`} href={`/brands/${brand.slug}`} key={brand.slug}><span>Brand partner</span><h2>{brand.title}</h2><p>{brand.description}</p><b>Explore brand ↗</b></Link>)}</div><div className="brands-cta"><h2>Building a brand in <em>MENA?</em></h2><Link className="about-button" href="/partner-with-us">Partner with us <span>↗</span></Link></div></section></main><SiteFooter /></>; }
