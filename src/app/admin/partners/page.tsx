"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
type Brand = { id: string; name: string; slug: string; website?: string; featured?: boolean; status: "published" | "draft"; displayOrder?: number };
export default function PartnersAdminPage() { const [brands, setBrands] = useState<Brand[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); useEffect(() => { fetch("/api/brands?sort=displayOrder&limit=100", { credentials: "include" }).then((r) => r.ok ? r.json() : Promise.reject(new Error("Unable to load partners."))).then((data) => setBrands(data.docs || [])).catch((e: Error) => setError(e.message)).finally(() => setLoading(false)); }, []); return <main className="cms-page"><div className="cms-page-heading"><div><span className="cms-kicker">Catalogue</span><h1>Partners</h1><p>Manage the brands and partners represented by Rasana.</p></div><Link className="primary-button" href="/admin/partners/new">+ Add partner</Link></div><section className="panel cms-table-panel">{loading && <p className="cms-table-message">Loading partners…</p>}{error && <p className="cms-table-message cms-table-error">{error}</p>}{!loading && !error && brands.length === 0 && <p className="cms-table-message">No partners yet. Add the first partner to begin.</p>}{brands.length > 0 && <div className="cms-data-table"><div className="cms-data-row cms-data-head"><span>Partner</span><span>Featured</span><span>Status</span><span>Order</span></div>{brands.map((brand) => <Link className="cms-data-row" href={`/admin/partners/${brand.id}`} key={brand.id}><strong>{brand.name}</strong><span>{brand.featured ? "Yes" : "No"}</span><span><i className={`cms-status-dot ${brand.status}`} />{brand.status}</span><span>{brand.displayOrder ?? 0}</span></Link>)}</div>}</section></main>; }
