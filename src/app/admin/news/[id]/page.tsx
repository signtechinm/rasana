"use client";
import { useEffect, useState } from "react";
import { useParams, useRouter } from "next/navigation";
import { NewsForm } from "../NewsForm";
export default function EditNewsPage() { const { id } = useParams<{ id: string }>(); const router = useRouter(); const [article, setArticle] = useState<Record<string, unknown>>({}); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); useEffect(() => { fetch(`/api/news/${id}`, { credentials: "include" }).then((r) => r.ok ? r.json() : Promise.reject(new Error("Article not found."))).then(setArticle).catch((e: Error) => setError(e.message)).finally(() => setLoading(false)); }, [id]); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); const data = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch(`/api/news/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, credentials: "include", body: JSON.stringify({ ...data, publishedAt: data.publishedAt || null, seo: { title: data.seoTitle, description: data.seoDescription } }) }); if (!response.ok) { setError("Could not save this article."); setSaving(false); return; } router.push("/admin/news"); } if (loading) return <main className="cms-page"><p className="cms-table-message">Loading article…</p></main>; return <NewsForm title="Edit article" description="Update the story, publication status, and metadata." article={article} onSubmit={submit} error={error} saving={saving} />; }
