"use client";
import { FormEvent, useEffect, useState } from "react";
export default function HomeAdminPage() { const [page, setPage] = useState<Record<string, string>>({}); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState(""); useEffect(() => { fetch("/api/pages?where[slug][equals]=home&limit=1", { credentials: "include" }).then((r) => r.json()).then((data) => setPage(data.docs?.[0] || {})).catch(() => setError("Home page content could not be loaded.")).finally(() => setLoading(false)); }, []); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setMessage(""); setError(""); const data = Object.fromEntries(new FormData(event.currentTarget)); const existingId = page.id; const response = await fetch(existingId ? `/api/pages/${existingId}` : "/api/pages", { method: existingId ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, credentials: "include", body: JSON.stringify({ title: "Home page", slug: "home", ...data }) }); if (!response.ok) setError("Could not save home page content."); else { setPage(await response.json()); setMessage("Home page content saved."); } setSaving(false); } if (loading) return <main className="cms-page"><p className="cms-table-message">Loading home page…</p></main>; return <main className="cms-page"><div className="cms-page-heading"><div><span className="cms-kicker">Content</span><h1>Home page</h1><p>Manage the core copy shown on the public home page.</p></div></div><form className="panel cms-form cms-form-wide" onSubmit={submit}><label>Hero heading<textarea name="homeHeroTitle" rows={3} defaultValue={page.homeHeroTitle || "Food and nutrition brands, supplied across the Emirates and the region."} /></label><label>Hero introduction<textarea name="homeHeroIntro" rows={4} defaultValue={page.homeHeroIntro || "Rasana International Trading is an independent supply partner based in Dubai South. We represent international food and supplement brands in the UAE and MENA markets."} /></label><label>Intro heading<textarea name="homeIntroTitle" rows={2} defaultValue={page.homeIntroTitle || "Food trade with a steadier point of view."} /></label><label>Intro body<textarea name="homeIntroBody" rows={4} defaultValue={page.homeIntroBody || "We say what we can deliver, and we deliver what we said. Supplier vetting, document control, registration and stock cover create dependable supply."} /></label><label>Closing CTA heading<textarea name="homeCtaTitle" rows={2} defaultValue={page.homeCtaTitle || "Start with an honest assessment."} /></label>{message && <p className="cms-success">{message}</p>}{error && <p className="cms-table-error">{error}</p>}<div className="cms-form-actions"><button className="primary-button" type="submit" disabled={saving}>{saving ? "Saving…" : "Save home page"}</button></div></form></main>; }
